_meta:
  type: "rolesmapping"
  config_version: 2

all_access:
  reserved: false
  users:
    - "{{ opensearch_admin_username }}"
  backend_roles:
    - "admin"

kibana_server:
  reserved: true
  users:
    - "{{ opensearch_kibanaserver_username }}"

kibana_user:
  reserved: false
  users:
    - "{{ opensearch_admin_username }}"

security_rest_api_access:
  users:
    - "{{ opensearch_admin_username }}"
