_meta:
  type: "internalusers"
  config_version: 2

{{ opensearch_admin_username }}:
  hash: "{{ opensearch_admin_password | password_hash('bcrypt') }}"
  reserved: true
  backend_roles:
    - admin
  description: "Admin user"

kibanaserver:
  hash: "{{ opensearch_kibanaserver_password | password_hash('bcrypt') }}"
  reserved: true
  description: "Dashboards internal user"
